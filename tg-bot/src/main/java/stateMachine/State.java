package stateMachine;

public enum State {
    REGISTER_NAME,
    MENU,
    MENU_CHOICE,
    VACATION,
    REPORT_TYPE,
    NOTIFICATION_CHOICE,
    CHOOSE_DAY,
    PARSE_DATE,
    SELECT_PROJECT,
    FINISH
}
